node {
    stage 'Checkout'
    git url: 'https://github.com/luisalbertogh/oracle-maven.git'
    
    stage 'Build'
    def mvnHome = tool 'maven35'
    bat "${mvnHome}/bin/mvn clean install"
    
    stage 'Archive'
    archiveArtifacts artifacts: 'target/*.jar', onlyIfSuccessful: true
}
